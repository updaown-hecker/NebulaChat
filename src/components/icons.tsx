import { Rocket } from 'lucide-react';
import type { LucideProps } from 'lucide-react';

export const NebulaChatLogo = (props: LucideProps) => (
  <Rocket {...props} />
);

// Add other custom icons here if needed
